# Option recommandée (LAN-friendly): ne fixe pas l'host, seulement le port.
# Le frontend déduira l'IP/hostname depuis l'URL du navigateur.
VITE_API_PORT=8000

# Optional UI label for the 4th DAF criterion
VITE_DAF_CRITERE4_LABEL="Critère 4"

# Option alternative (si tu veux forcer une URL complète):
# VITE_API_URL="http://192.168.1.10:8000/api"
